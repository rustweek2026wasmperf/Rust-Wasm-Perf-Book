name = "rust-wasm-myths"
compatibility_date = "2025-05-01"
workers_dev = true

# Serve `book-dist` as a static site via Workers Static Assets.
# Deploy with: `npx wrangler deploy` (after building with REPO_PATH="").
[assets]
directory = "./book-dist"
not_found_handling = "404-page"
